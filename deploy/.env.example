# 复制成 .env 后填入自己的值： cp .env.example .env
# 两条都要换成随机长串，例如： openssl rand -hex 24
#
# 管理密码：只有你自己知道，用来登录面板（查看渠道与上游 key）。
LAPI_ADMIN_PASSWORD=
# 用户 key：发给使用者，填在工具的 key 位置，只能调用 /v1 转发。
LAPI_GATEWAY_TOKEN=
# 网关监听端口（容器内），需与 docker-compose.yml 里映射的右侧一致。
LAPI_PORT=8787
# 可选：国内服务器构建时依赖安装很慢/超时，取消下面这行的注释换成国内源。
# NPM_REGISTRY=https://registry.npmmirror.com
# 可选：服务器内存紧张（例如 1.6G 且已跑别的服务）时设为 1，镜像内不再跑 vite：
#   本机执行 pnpm build，把 web/dist 一起上传后再 docker compose up -d --build
# SKIP_WEB_BUILD=1
